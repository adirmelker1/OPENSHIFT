kind: Template
apiVersion: template.openshift.io/v1
metadata:
  name: my-java-template
objects:
  - apiVersion: apps/v1
    kind: Deployment
    metadata:
      annotations:
        image.openshift.io/triggers: '[{"from":{"kind":"ImageStreamTag","name":"${SOURCE_REPOSITORY_URL}:latest","namespace":"discoverprogramming-dev"},"fieldPath":"spec.template.spec.containers[?(@.name==\"${SOURCE_REPOSITORY_URL}\")].image"}]'
        openshift.io/generated-by: OpenShiftNewApp
      labels:
        app: ${SOURCE_REPOSITORY_URL}
        app.kubernetes.io/component: ${SOURCE_REPOSITORY_URL}
        app.kubernetes.io/instance: ${SOURCE_REPOSITORY_URL}
      name: ${SOURCE_REPOSITORY_URL}
      namespace: discoverprogramming-dev
    spec:
      progressDeadlineSeconds: 600
      replicas: 1
      revisionHistoryLimit: 10
      selector:
        matchLabels:
          deployment: ${SOURCE_REPOSITORY_URL}
      strategy:
        rollingUpdate:
          maxSurge: 25%
          maxUnavailable: 25%
        type: RollingUpdate
      template:
        metadata:
          labels:
            deployment: ${SOURCE_REPOSITORY_URL}
        spec:
          containers:
          - env:
            - name: SPRING_CONFIG_LOCATION
              value: /config/
            image: discoverprogramming-dev/${SOURCE_REPOSITORY_URL}:latest
            imagePullPolicy: IfNotPresent
            name: ${SOURCE_REPOSITORY_URL}
            ports:
            - containerPort: 8080
              protocol: TCP
            resources: {}
            terminationMessagePath: /dev/termination-log
            terminationMessagePolicy: File
            startupProbe:
              httpGet:
                path: /todos
                port: 8080
                scheme: HTTP
              timeoutSeconds: 1
              periodSeconds: 10
              successThreshold: 1
              failureThreshold: 3
            volumeMounts:
            - mountPath: /config
              name: ${SOURCE_REPOSITORY_URL}-volume
          dnsPolicy: ClusterFirst
          restartPolicy: Always
          schedulerName: default-scheduler
          securityContext: {}
          terminationGracePeriodSeconds: 30
          volumes:
          - configMap:
              defaultMode: 420
              name: ${SOURCE_REPOSITORY_URL}-cm
            name: ${SOURCE_REPOSITORY_URL}-volume

  - apiVersion: v1
    kind: Service
    metadata:
      labels:
        app: ${SOURCE_REPOSITORY_URL}
        app.kubernetes.io/component: ${SOURCE_REPOSITORY_URL}
        app.kubernetes.io/instance: ${SOURCE_REPOSITORY_URL}
      name: ${SOURCE_REPOSITORY_URL}
      namespace: discoverprogramming-dev
    spec:
      internalTrafficPolicy: Cluster
      ipFamilies:
      - IPv4
      ipFamilyPolicy: SingleStack
      ports:
      - name: 8080-tcp
        port: 8080
        protocol: TCP
        targetPort: 8080
      selector:
        deployment: ${SOURCE_REPOSITORY_URL}
      sessionAffinity: None
      type: ClusterIP
    status:
      loadBalancer: {}

  - apiVersion: route.openshift.io/v1
    kind: Route
    metadata:
      creationTimestamp: null
      labels:
        app: ${SOURCE_REPOSITORY_URL}
        app.kubernetes.io/component: ${SOURCE_REPOSITORY_URL}
        app.kubernetes.io/instance: ${SOURCE_REPOSITORY_URL}
      name: ${SOURCE_REPOSITORY_URL}
    spec:
      port:
        targetPort: 8080-tcp
      to:
        kind: ""
        name: ${SOURCE_REPOSITORY_URL}
        weight: null
    status: {}

parameters:
  - name: APPLICATION_NAME 
    displayName: Application name
    description: Application name
    value: ""
    required: true 